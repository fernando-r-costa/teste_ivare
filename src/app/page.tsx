import Painel from "./painel/page";

// Página principal da aplicação, renderiza o componente Painel para exibir e gerenciar os pedidos.
export default function Home() {
  return (
    <Painel/>
  );
}
